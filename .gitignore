# Reference materials — kept locally for context, not part of Propeller
other_issuing_project/
other_hypercone_project/
*.pdf
paystack-doc.md

# Node
node_modules/
dist/
build/
.next/
*.log
.npm
.yarn-integrity
.yarn/cache
.yarn/install-state.gz
.pnp.*

# Local env (production secrets live in Rune, never on disk)
.env
.env.local
.env.*.local

# OS
.DS_Store
Thumbs.db

# Editors
.vscode/
.idea/
*.swp
*.swo

# Build artifacts
*.tsbuildinfo
coverage/
.turbo/
.cache/

# Test
*.lcov

# Rune CLI config (per-developer)
.rune/

# Terraform
**/.terraform/
*.tfstate
*.tfstate.*
*.tfplan
.terraform.lock.hcl

# Secrets — defensive; nothing sensitive should ever be committed
*.pem
*.key
secrets/

# Allow PEM/key fixtures inside test directories
!**/fixtures/**/*.pem
!**/fixtures/**/*.key
!**/test/fixtures/**


_docs/